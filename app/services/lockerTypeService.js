const Checker = require('../common/checker');
const Constants = require('../common/constants');
const CustomError = require('../common/error/customError');

module.exports = {
  createLockerType: async(lockerTypeData, transaction) => {

  },

  updateLockerType: async(lockerTypeData, transaction) => {

  },

  retrieveLockerTypes: async(id) => {

  },

  retrieveAllLockerType: async() => {

  },

  disableLockerType: async(id) => {

  },

  deleteLockerType: async(id) => {

  }
}